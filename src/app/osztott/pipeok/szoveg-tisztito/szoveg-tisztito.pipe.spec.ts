import { SzovegTisztitoPipe } from './szoveg-tisztito.pipe';

describe('SzovegTisztitoPipe', () => {
  it('create an instance', () => {
    const pipe = new SzovegTisztitoPipe();
    expect(pipe).toBeTruthy();
  });
});
